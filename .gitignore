# Ignore build directories and large binary files
cmake-build-debug/
cmake-build-release/

# Audio plugins generated by JUCE (if applicable)
*.vst3
*.vst
*.aaxplugin
*.au

# JUCE module subprojects (if cloned locally)
JUCE/

# Ignore large media or audio files (if using Git LFS, otherwise track specific ones)
*.wav
*.mp3
*.bin
*.zip

# OS-specific files (macOS, Windows)
__MACOSX/